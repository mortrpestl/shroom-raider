# **AI Usage**

Most of our AI usage is for insanely tedious tasks (e.g. getter methods) or errors that we have not been able to solve even after hours of StackOverFlow or Reddit discussions.

Current results of these prompts are the following:

- Printing non-ASCII characters in the terminal  
  - Prompt: Python prints weird characters in my terminal when output contains non-ASCII. How to fix?

NOTE: In older versions of the code (that no longer appear in any part of the repo), some of us used Github Copilot to generate some getter and setter methods. However, this has since been replaced with parts that were made by humans after some big refactoring.